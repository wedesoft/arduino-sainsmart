dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install arduino-mk google-mock

test:
  override:
    - cp calibration.hh.default calibration.hh
    - env PATH=/usr/share/arduino:$PATH make all-recursive
    - make check-controller
